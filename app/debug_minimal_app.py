"""
Debug file (disabled).

This file used to define a FastAPI app for quick tests. To avoid multiple
FastAPI app instances in the project, this debug helper has been disabled.

If you need a standalone test app, create a new file outside `app/`.
"""
